package graphql.gitlab.model;

import java.util.List;
import java.util.Objects;

import org.eclipse.microprofile.graphql.Name;

import graphql.gitlab.GitLabPayloadResponse;

/**
 * Autogenerated return type of DestroyBoard.
 */
@Name("DestroyBoardPayload")
public class DestroyBoardPayload implements GitLabPayloadResponse {

    /**
     * Board after mutation.
     */
    private Board board;

    /**
     * A unique identifier for the client performing the mutation.
     */
    private String clientMutationId;

    /**
     * Errors encountered during execution of the mutation.
     */
    private List<String> errors;

    public Board getBoard() {
        return board;
    }

    public DestroyBoardPayload setBoard(Board board) {
        this.board = board;
        return this;
    }

    public String getClientMutationId() {
        return clientMutationId;
    }

    public DestroyBoardPayload setClientMutationId(String clientMutationId) {
        this.clientMutationId = clientMutationId;
        return this;
    }

    public List<String> getErrors() {
        return errors;
    }

    public DestroyBoardPayload setErrors(List<String> errors) {
        this.errors = errors;
        return this;
    }

    @Override
    public int hashCode() {
        return Objects.hash(board, clientMutationId, errors);
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        DestroyBoardPayload other = (DestroyBoardPayload) obj;
        return Objects.equals(board, other.board) && Objects.equals(clientMutationId, other.clientMutationId) && Objects.equals(errors, other.errors);
    }

    @Override
    public String toString() {
        return "DestroyBoardPayload [board=" + board + ", clientMutationId=" + clientMutationId + ", errors=" + errors + "]";
    }

}
