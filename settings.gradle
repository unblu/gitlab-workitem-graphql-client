plugins {
    // Auto-provisions the JDK requested by the java toolchain (build.gradle)
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name='gitlab-workitem-graphql-client'
